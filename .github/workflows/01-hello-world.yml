name: 01 · Hello World

# Smallest possible workflow. One job, one step.
# Trigger: manual button only, so the catalogue does not auto-run on every push.
on:
  workflow_dispatch:

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello from GitHub Actions on $(hostname) at $(date -u)"
